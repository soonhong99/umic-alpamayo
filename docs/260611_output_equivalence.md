# 260611 출력 등가성 게이트 — PASS

**날짜**: 2026-06-11
**스크립트**: `scripts/260611_output_equivalence.py`
**프로토콜**: 한 프로세스에서 eager → 전체 UMIC 스택(융합 8종 + InplaceKVCache + decode
CUDA Graph) 적용 → UMIC 2회(캡처 run + replay run). 모두 seed 42, near-greedy(temp 1e-4),
동일 입력. 비교: 토큰 시퀀스 / waypoint 거리 / replay 결정론.

## 결과

| 검증 항목 | 결과 |
|----------|------|
| 토큰 시퀀스 (eager vs UMIC) | **3,106 토큰 전부 일치** — argmax 단 한 번도 안 갈림 |
| 궤적 ADE (eager vs UMIC) | **0.0038 m (3.8 mm)** |
| 궤적 FDE / max | 0.0150 m / 0.0150 m (6.4 s 끝점에서 1.5 cm) |
| replay 결정론 (capture vs replay run) | **비트 동일 (ADE 0.0000)** — graph가 비결정성 없음 |

## 해석

- 커널별 bf16 오차(1e-3~8e-3)가 36 layer × 4 stage를 거치며 누적돼도 **토큰 결정을 단
  한 번도 뒤집지 못했고**, 궤적 차이는 6.4초 주행 끝점에서 1.5 cm — 차선 폭(~3.5 m)의
  0.4% 수준으로 주행 의사결정에 무의미.
- 등가성의 근본 이유: 융합 커널들이 eager와 **같거나 더 높은 내부 정밀도**(fp32 레지스터)
  로 같은 수학을 수행하기 때문 (RoPE는 fp32 GT 대비 eager보다 정확함을 별도 확인).
- 이로써 공식 주장 완성: **"모델 무수정·비양자화로 동일 조건 −29.8% (3,846→2,701 ms),
  출력 등가(토큰 일치, ADE 3.8 mm), replay 결정론 보장."**

## 한계

- 단일 클립(030c760c) 1개 입력에서의 등가성 — 다양한 장면(야간/우천/long-tail)으로
  표본을 늘리면 주장이 강해진다. 단 등가성의 메커니즘(동일 수학, 동등 이상 정밀도)이
  입력 의존적이지 않으므로 일반화 기대는 합리적.
